<%= render ("layouts/navbar.html.erb") %>

<div class="container">
  <h1><%= @stop.station.name %>: <%= @stop.line.name %></h1>

  <%= render("stops/form.html.erb") %>

  <p><%= link_to "Delete", stop_path(@stop),
                           :data => {:confirm => "You sure?",
                                     :method => "delete"},
                           :class => "btn btn-danger" %></p>
</div>
